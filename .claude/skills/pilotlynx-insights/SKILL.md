---
name: pilotlynx-insights
description: View cross-project insights from the self-improvement loop
argument-hint: [--last <n>] [--since <date>]
allowed-tools: Bash
---

View cross-project insights:

```bash
npx pilotlynx insights $ARGUMENTS
```

Shows insights generated by `pilotlynx improve`. Options: `--last <n>` (default 5), `--since YYYY-MM-DD`.
